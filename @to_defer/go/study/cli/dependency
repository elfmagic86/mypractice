# package

## 명령

- 현재위치 기준 필요 의존성 전체 설치
https://stackoverflow.com/questions/45426891/why-i-am-not-able-to-fetch-go-sdk-for-docker
```sh
# Get all imports references in this directory and the expanded slice
# consisting of all subdirectories, recursively
go get -v -t ./...
```

- 특정 의존성 GOPATH에 설치
```
# gopath에 전체소스가 다운되고, bin에 바이너리도 생성됨
# -u 옵션: network 사용하여 명명된 패키지 갱신
go get github.com/golang/example/hello
```

## memo

- remote import path
  https://golang.org/cmd/go/#hdr-Remote_import_paths
  > TODO 모듈관련내용

- TODO 외부 소스 버전관리
