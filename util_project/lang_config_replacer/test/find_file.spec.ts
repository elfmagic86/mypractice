import test from 'ava';
// import * as path from 'path';
// import * as fileUtil from '@app/util/fileUtil';

// const filePath = path.resolve(__dirname, './samples/s');

test.before(async _ => {
	// await fileUtil.exists(filePath);
});
test.after(async _ => {
	// await fileUtil.deleteFile(filePath);
});

