
# 뷰 에 대해서
	- 물리적 공간을 할당하는가.
	: oracle O, mysql X

	- 뷰는 테이블보다는 매크로에 가깝다.
		```
		# 이런 테이블이라 말하기는 거리가먼 뷰도 생성가능하기때문
		CREATE VIEW test_view AS ( select 1 );
		```

	- 뷰의 용도
		- 보안
		: 테이블, 뷰에 권한 부여할수있으므로 특정테이블로 여러 형태의 뷰를 만듬. 권한을 다르게 하여 제공하여 보안유지.
