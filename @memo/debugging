

* 유닉스 툴
	- dd: convert and copy file
	jot

	- `sort -u`: sorting unique


	- 원하는것찾기..
	: grep으로 원하는 라인찾고 grep -v로 불필요 제거 후, awk로 각 행에서 원하는 필드만 골라냄

	- 각항목의 개수 파악
	uniq -c
	- 결과중 가장많이 등장한 항목목록
	sort -n

	- 비교
	diff
	comm: 두개의 정렬된 리스트 비교

	- `wc -l`
	라인의 전체개수

	-  git blame --line-porcelain {파일명}
	파일의 각 라인별 최종수정사항

	- 목록의 각각 에 대해 명령수행하기 패턴
	: find 후 xargs로 실행
		- -0, -print0: 공백이 담긴 파일이름을 처리? 공백대신 null문자로 끝나도록 지정하는 옵션
			ex) 목록1
				목 공백 2  // 이때 공백으로 라인구분하지않아야함

* bash
	<( 구문
	: 명령에서 파일 형태의 인수를 받는자리에서 파일명 대신에 프로세스를 인수로 지정할 수 있다.
	  괄호안에 지정한 프로세스는 백그라운드 실행된 후 결과가 파이프로 연결되어 명령의 인수로 전달됨
	    그러면 프로세스의 stdout을 내용으로 사용하겟지

