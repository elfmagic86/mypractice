


# dump
	- export to name.sql (using ssms)
	: http://statmap.co.uk/?page_id=9207
	> 저장시 advance 옵션에서 `스키마 및 데이터`를 선택해야 데이터가 포함됨

	- import from name.sql (using sqlcmd)
		```powershell
		# ssms에서 수작업으로 쿼리파일 실행할 경우, 용량 큰 것은 메모리 부족에러남(일괄 밀어넣기의 버퍼에서 실패하는것으로 추정됨)
		sqlcmd -S DESKTOP-BJ84124 -d mc_gis -i ./Table.sql
		```

# mssql to mysql (using workbench)
: 로컬작업

	- odbc 설정
	odbc -> 사용자 dns -> 드라이버(ODBC driver 13 for SQL Server)추가
	-> 서버이름 주의(mssql의 서버이름) 후, 적절히 내용채우고 완료

	- workbench
	menu -> Database -> migration wizard -> start migration
	하단우측의 next버튼따라가면서 내용 채우기.

		* !! mssql테이블이 db.table의 형태로 저장되기에 mysql연결시, 해당 db에 권한있는 유저여야함


		* !! Manual Editing 단계에서 컬럼매핑 수정
			컬럼타입 등 미리해결안하면..복사시 에러남


=> 삽질..그냥 수작업으로. TODO 커맨드라인으로 작업할수있는 환경이 필요하다
	1. export csv
	2. 스키마는 마이그레이션 위자드로 생성되는 것 타입 적절히 변경하여 사용.
	3. import csv



# ssms 이슈
	- 컬럼 추가후 해당 컬럼사용하면 열정보가 없다는 에러나옴
	편집" -> 인텔리 센스 -> 로컬 캐쉬 새로고침


# ssms에서 export 기능 사용
localhost -> 플랫(|로 구분된)텍스트파일
	- 1. 원본
	Data Source: SQL Server Native Client 11
	Server Name: localhost

	Database: dbname
	- 2. 플랫... csv같은것

